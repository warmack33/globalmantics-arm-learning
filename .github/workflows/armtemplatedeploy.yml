name: armtemplatedeployment

on:
 
  workflow_dispatch:
jobs:
  armdeploy:
    runs-on: ubuntu-latest

      steps:
            - uses: actions/checkout@v3

            - name: Azure Login
              uses: Azure/Login@v1
              with:
                creds:${{secret.AZURE_CRDENTIALS}}
